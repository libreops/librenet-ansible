# {{ ansible_managed }}

# TYPE  DATABASE        USER            ADDRESS                 METHOD

# "local" is for Unix domain socket connections only
local   all             all                                     peer

# IPv4 local connections:
host    diaspora_production     diaspora        127.0.0.1/32    trust
host    diaspora_production     prosody         127.0.0.1/32    trust
